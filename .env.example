# Docker Compose environment variables
# Copy to .env and update values before running: cp .env.example .env

# Redis password (REQUIRED — used by both redis and file-organizer services)
REDIS_PASSWORD=changeme
